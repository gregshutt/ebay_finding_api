require "spec_helper"

describe EbayFindingApi do
  it "has a version number" do
    expect(EbayFindingApi::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
